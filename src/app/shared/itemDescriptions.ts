export class homepage{
    id:number;
    itemTitle:string;
    desc:string;

    constructor(id:number, itemTitle:string, desc:string) {
        this.id = id;
        this.itemTitle = itemTitle;
        this.desc = desc;
    }
}